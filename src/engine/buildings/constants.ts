export enum Direction {
  DEG_0 = 0,
  DEG_90 = 1,
  DEG_180 = 2,
  DEG_270 = 3,
}

export enum BuildingColor {
  COMMON = 0,
  RED = 1,
  BROWN = 2,
  OLIVE = 3,
}

export enum BuildingStyle {
  MAIN_FLOOR = 0,
  MIDDLE_FLOORS = 1,
  ROOF = 2,
}

export enum BuildingRoofType {
  FLAT = 0,
  GABLE = 1,
}
